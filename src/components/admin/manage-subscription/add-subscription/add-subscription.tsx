import React from "react";
import CommonSubscription from "../common/common-subscription";

const AddSubscription = () => {
  return (
    <div>
      <CommonSubscription edit={false} />
    </div>
  );
};

export default AddSubscription;
